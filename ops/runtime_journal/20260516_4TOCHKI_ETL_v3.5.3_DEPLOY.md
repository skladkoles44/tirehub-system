# 4TOCHKI — ETL v3.5.3 DEPLOY

**UTC:** 
**Статус:** ✅

---

## 1. Что произошло

ETL-скрипт обновлён с v3.2 до v3.5.3. Проведён цикл оптимизаций и исправлений.

## 2. Версии

| Версия | Ключевые изменения |
|--------|-------------------|
| v3.3 | separators, BATCH_SIZE=2000, NOW() in SQL, isinstance optimization, CHUNK_SIZE=120 |
| v3.4 | orjson, streaming insert, partial indexes, pg_notify |
| v3.4.1 | sequential stock (psycopg2 thread-safety) |
| v3.5.0 | run_id, JsonFormatter, unified log() |
| v3.5.1 | logger.exception with error text, fetchmany(5000) |
| v3.5.3 | threading.Event() for shutdown, signal handler safe |

## 3. Текущая конфигурация

- CHUNK_SIZE: 120
- BATCH_SIZE: 2000
- Catalog: parallel (6 workers)
- Stock: sequential streaming
- Log: /var/log/4tochki_etl_{mode}.log (JSON, run_id)
- Indexes: idx_offers_active_price, idx_products_active

## 4. Результат

v3.5.3 задеплоен на VPS. Синтаксис проверен. Готов к тестовому запуску.
